extends layout 

block content 
 h1 #{title}: #{item.name}
 p Do you reall want to delete this item ? 
 form(method='POST')
    div 
     input(type='hidden' , name='itemid', value=item._id)
    div.form-group
      label(for='password') Password:
      input(type='password', name='password',id="password", required)
    button(type='submit') Delete
 if errors 
   ul 
   for error in errors
    li!= error.msg